% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension-table.R
\name{add_table_extension}
\alias{add_table_extension}
\title{Add Table Extension to a STAC Item}
\usage{
add_table_extension(
  item,
  columns = NULL,
  primary_geometry = NULL,
  row_count = NULL,
  storage_options = NULL,
  asset_key = NULL
)
}
\arguments{
\item{item}{A STAC Item object created with \code{stac_item()}.}

\item{columns}{(list, optional) A list of column objects created with
\code{table_column()}, one entry per column in the table.}

\item{primary_geometry}{(character, optional) The name of the column that
holds the primary geometry, for use by libraries such as geopandas or
\code{sf} when a table has multiple geometry columns.}

\item{row_count}{(numeric, optional) The number of rows in the dataset.}

\item{storage_options}{(list, optional) Additional keywords needed to open
the dataset (e.g. for \code{fsspec}-style access). This is an asset-level
field, so \code{asset_key} must also be provided when supplying it.}

\item{asset_key}{(character, optional) The asset to attach
\code{storage_options} to. Required when \code{storage_options} is provided.
\code{columns}, \code{primary_geometry}, and \code{row_count} are always written to
item properties, matching the Table Extension specification.}
}
\value{
The modified STAC Item with Table extension fields added.
}
\description{
Adds the Table Extension to a STAC Item. The Table Extension provides
fields to describe tabular datasets (e.g. GeoParquet, CSV, flat
GeoPackage/SQLite tables) referenced by an Item, including the columns
present, which column holds the primary geometry, and the number of rows.
}
\details{
\subsection{Extension Schema URI}{

The Table Extension v1.2.0 schema URI is:
\verb{https://stac-extensions.github.io/table/v1.2.0/schema.json}
}

\subsection{Field Placement}{

\code{table:columns}, \code{table:primary_geometry}, and \code{table:row_count} are Item
(and Collection) properties fields. \code{table:storage_options} is an
asset-level field, so it is always attached to the asset identified by
\code{asset_key}.
}

\subsection{Column Object Fields}{

Each entry in \code{columns} is created with \code{table_column()} and can include:
\itemize{
\item \code{name}: The column name (required)
\item \code{description}: Description of the column
\item \code{type}: Data type of the column. If the underlying file format has a
type system (e.g. Parquet), it is recommended to use those type names.
}
}

\subsection{Recommended Companion Extensions}{

The Table extension is often used with the \strong{Projection} extension, to
describe the coordinate reference system and spatial bounds of the table.
}
}
\examples{
item <- stac_item(
  id = "my-parquet-dataset",
  geometry = list(
    type = "Polygon",
    coordinates = list(list(
      c(-105.5, 39.5), c(-104.5, 39.5), c(-104.5, 40.5),
      c(-105.5, 40.5), c(-105.5, 39.5)
    ))
  ),
  bbox = c(-105.5, 39.5, -104.5, 40.5),
  datetime = "2023-06-15T10:30:00Z"
)

cols <- list(
  table_column(name = "geometry", type = "binary", description = "Point geometry"),
  table_column(name = "id", type = "int64"),
  table_column(name = "value", type = "double")
)

item <- item |>
  add_asset(
    "data",
    href = "https://example.com/data.parquet",
    type = "application/x-parquet",
    roles = c("data")
  ) |>
  add_table_extension(
    columns = cols,
    primary_geometry = "geometry",
    row_count = 15000,
    storage_options = list(anon = TRUE),
    asset_key = "data"
  )

}
\references{
Table Extension Specification:
\url{https://github.com/stac-extensions/table}
}
\seealso{
\itemize{
\item \code{\link[=table_column]{table_column()}} for creating column objects
\item \code{\link[=stac_item]{stac_item()}} for creating STAC Items
}
}
