% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{validate_stac_schema}
\alias{validate_stac_schema}
\title{Validate a STAC Object Against the Official JSON Schema}
\usage{
validate_stac_schema(stac_object, validate_extensions = TRUE)
}
\arguments{
\item{stac_object}{A STAC object created with \code{\link[=stac_item]{stac_item()}},
\code{\link[=stac_collection]{stac_collection()}}, or \code{\link[=stac_catalog]{stac_catalog()}}.}

\item{validate_extensions}{(logical) If \code{TRUE} (the default), validate the
serialised object against every JSON Schema URI declared in the object's
\code{stac_extensions} field in addition to the core STAC schema.}
}
\value{
A named list with elements \code{valid}, \code{errors}, and \code{warnings}.
}
\description{
Validates a STAC Catalog, Collection, or Item against the official STAC
JSON Schemas hosted at \code{schemas.stacspec.org}, using the
\href{https://CRAN.R-project.org/package=jsonvalidate}{jsonvalidate} package.
Unlike \code{\link[=validate_stac]{validate_stac()}}, which applies hand-written structural checks,
this function performs authoritative validation against the exact schema
that defines the specification.

When \code{validate_extensions = TRUE} (the default), each URI listed in the
object's \code{stac_extensions} field is also used as a JSON Schema and
validated against in turn, so extension-level fields are checked as well.
}
\details{
\subsection{Requirements}{

The \code{jsonvalidate} package must be installed:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{install.packages("jsonvalidate")
}\if{html}{\out{</div>}}
}

\subsection{Network Access}{

Schema files are fetched over the network at validation time — both the
core STAC schema and any extension schemas. Internet access is required.
Validation will fail with an informative error message if a schema URL
cannot be reached.
}

\subsection{Schema URLs}{

The core schema URL is derived from the object type and its \code{stac_version}
field, following the pattern:
\verb{https://schemas.stacspec.org/v\{stac_version\}/\{type\}-spec/json-schema/\{type\}.json}
}

\subsection{Return Value}{

Returns the same structure as \code{\link[=validate_stac]{validate_stac()}}:
\itemize{
\item \code{valid} — \code{TRUE} if no schema errors were found.
\item \code{errors} — character vector of error messages, one per violation.
Extension errors are prefixed with the extension schema URI.
\item \code{warnings} — always an empty character vector (reserved for future use).
}
}
}
\examples{
item <- stac_item(
  id       = "my-item",
  geometry = list(type = "Point", coordinates = c(-105, 40)),
  bbox     = c(-105, 40, -105, 40),
  datetime = "2023-01-01T00:00:00Z"
)

\dontrun{
result <- validate_stac_schema(item)
result$valid
result$errors
}

}
\references{
Official STAC JSON Schemas: \url{https://schemas.stacspec.org}
}
\seealso{
\code{\link[=validate_stac]{validate_stac()}} for fast, offline structural checks.
}
