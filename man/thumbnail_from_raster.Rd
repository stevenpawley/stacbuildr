% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thumbnail.R
\name{thumbnail_from_raster}
\alias{thumbnail_from_raster}
\title{Generate a Thumbnail PNG from a Stars Raster Object}
\usage{
thumbnail_from_raster(
  stars_obj,
  path,
  width = 256,
  height = 256,
  title = NULL,
  ...
)
}
\arguments{
\item{stars_obj}{A \code{stars} object.}

\item{path}{(character, required) File path for the output PNG.}

\item{width}{(integer) Image width in pixels. Default is 256.}

\item{height}{(integer) Image height in pixels. Default is 256.}

\item{title}{(character, optional) Title for the returned asset.}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
A STAC asset list with \code{href}, \code{type = "image/png"}, and
\code{roles = c("thumbnail")}.
}
\description{
Renders a \code{stars} raster object to a PNG image and returns a STAC asset
pointing to it. Multi-band rasters with 3 or more bands are rendered as an
RGB composite using the first three bands; single-band rasters are rendered
as greyscale.
}
\examples{
\dontrun{
library(stars)

r <- read_stars(system.file("tif/L7_ETMs.tif", package = "stars"))
asset <- thumbnail_from_raster(r, path = "thumbnail.png")

item <- item_from_raster(r, href = "image.tif", datetime = "2023-01-01T00:00:00Z")
item <- add_asset(item, key = "thumbnail", asset = asset)
}

}
