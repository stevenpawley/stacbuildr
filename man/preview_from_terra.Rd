% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thumbnail.R
\name{preview_from_terra}
\alias{preview_from_terra}
\title{Generate a Thumbnail PNG from a Terra SpatRaster Object}
\usage{
preview_from_terra(
  terra_obj,
  path,
  width = 256,
  height = 256,
  title = NULL,
  ...
)
}
\arguments{
\item{terra_obj}{A \code{SpatRaster} object (from the \code{terra} package).}

\item{path}{(character, required) File path for the output PNG.}

\item{width}{(integer) Image width in pixels. Default is 256.}

\item{height}{(integer) Image height in pixels. Default is 256.}

\item{title}{(character, optional) Title for the returned asset.}

\item{...}{Additional arguments passed to \code{terra::plotRGB()} or \code{terra::plot()}.}
}
\value{
A STAC asset list with \code{href}, \code{type = "image/png"}, and
\code{roles = c("overview")}.
}
\description{
Renders a \code{SpatRaster} object to a PNG image and returns a STAC asset
pointing to it. Multi-band rasters with 3 or more bands are rendered as an
RGB composite using the first three bands; single-band rasters are rendered
as greyscale.
}
\examples{
\dontrun{
library(terra)

r <- rast(system.file("ex/logo.tif", package = "terra"))
asset <- preview_from_terra(r, path = "thumbnail.png")

item <- item_from_terra(r, href = "image.tif",
                        datetime = "2023-01-01T00:00:00Z")
item <- add_asset(item, key = "thumbnail", asset = asset)
}

}
