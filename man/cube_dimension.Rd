% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension-datacube.R
\name{cube_dimension}
\alias{cube_dimension}
\title{Create a Datacube Dimension Object}
\usage{
cube_dimension(
  type,
  extent = NULL,
  values = NULL,
  step = NULL,
  unit = NULL,
  reference_system = NULL,
  description = NULL,
  axis = NULL,
  axes = NULL,
  bbox = NULL,
  geometry_types = NULL,
  ...
)
}
\arguments{
\item{type}{(character, required) The type of the dimension. One of
\code{"spatial"} (horizontal x/y or vertical z axis), \code{"geometry"} (vector
data cube dimension), \code{"temporal"}, or a custom string for an
"additional" dimension (e.g. \code{"spectral"}, \code{"pressure"}). Custom values
must not be \code{"spatial"} or \code{"geometry"}.}

\item{extent}{(numeric or character, optional/required) The extent of the
dimension as \code{c(min, max)}. Required for horizontal spatial (\code{x}/\code{y})
and temporal dimensions. For vertical (\code{z}) and additional dimensions,
either \code{extent} or \code{values} must be given. \code{NA} may be used for an
open-ended bound.}

\item{values}{(numeric or character, optional) An explicit, potentially
irregularly spaced, list of values in the dimension, used instead of or
in addition to \code{extent}.}

\item{step}{(numeric or character, optional) The distance between two
consecutive values, e.g. the spatial resolution or an ISO 8601 duration
for a temporal dimension. \code{NULL}/absent means irregular spacing.}

\item{unit}{(character, optional) The unit of measurement for the values
and extent.}

\item{reference_system}{(optional) The spatial (or other) reference
system, e.g. an EPSG code, WKT2 string, or PROJJSON object. Applies to
\code{"spatial"} and \code{"geometry"} dimensions. Defaults to EPSG:4326 per the
spec if omitted for spatial dimensions.}

\item{description}{(character, optional) Detailed description of the
dimension. CommonMark 0.29 syntax may be used for rich text
representation.}

\item{axis}{(character, required for \code{type = "spatial"}) The axis of the
spatial dimension: \code{"x"}, \code{"y"}, or \code{"z"}.}

\item{axes}{(character, optional) For \code{type = "geometry"} dimensions, the
axes that the \code{bbox} and geometries are given in, e.g. \code{c("x", "y")}.
Defaults to \code{c("x", "y")} per the spec if omitted.}

\item{bbox}{(numeric, required for \code{type = "geometry"}) The bounding box
of the geometries as \code{c(xmin, ymin, xmax, ymax)} (or with a \code{z} axis).}

\item{geometry_types}{(character, optional) For \code{type = "geometry"}
dimensions, the allowed GeoJSON geometry types (e.g. \code{"Point"},
\code{"Polygon"}).}

\item{...}{Additional fields for the dimension object.}
}
\value{
A named list of class \code{"cube_dimension"}.
}
\description{
Creates a dimension object for use with the Datacube Extension. Describes
a single dimension of an N-dimensional data cube, such as an \code{x}/\code{y}
spatial axis, a vertical axis, a temporal axis, a geometry (vector data
cube) dimension, or an additional custom dimension (e.g. wavelength,
pressure level).
}
\details{
\subsection{Dimension Types}{
\itemize{
\item \strong{Horizontal spatial} (\code{type = "spatial"}, \code{axis = "x"} or \code{"y"}):
\code{extent} is required.
\item \strong{Vertical spatial} (\code{type = "spatial"}, \code{axis = "z"}): \code{extent} or
\code{values} is required.
\item \strong{Geometry} (\code{type = "geometry"}): describes a vector data cube
dimension; \code{bbox} is required.
\item \strong{Temporal} (\code{type = "temporal"}): \code{extent} is required, given as ISO
8601 datetime strings (or \code{NA} for an open bound).
\item \strong{Additional} (any other \code{type}, e.g. \code{"spectral"}): a custom
dimension such as wavelength or pressure level; \code{extent} or \code{values} is
required.
}
}
}
\examples{
# Horizontal spatial dimensions
x_dim <- cube_dimension(
  type = "spatial", axis = "x", extent = c(-105.5, -104.5),
  reference_system = 4326
)
y_dim <- cube_dimension(
  type = "spatial", axis = "y", extent = c(39.5, 40.5),
  reference_system = 4326
)

# Temporal dimension
time_dim <- cube_dimension(
  type = "temporal",
  extent = c("2023-06-01T00:00:00Z", "2023-06-30T00:00:00Z"),
  step = "P1D"
)

# Additional dimension (e.g. spectral band index)
band_dim <- cube_dimension(
  type = "bands",
  values = c("B02", "B03", "B04", "B08")
)

}
