% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension-datacube.R
\name{cube_variable}
\alias{cube_variable}
\title{Create a Datacube Variable Object}
\usage{
cube_variable(
  type,
  dimensions = character(0),
  extent = NULL,
  values = NULL,
  unit = NULL,
  nodata = NULL,
  data_type = NULL,
  description = NULL,
  ...
)
}
\arguments{
\item{type}{(character, required) The type of the variable: \code{"data"} for
the primary data variable(s) or \code{"auxiliary"} for supporting variables
(e.g. quality flags, coordinate variables).}

\item{dimensions}{(character, required) A character vector of dimension
keys (matching names used in \code{dimensions} passed to
\code{add_datacube_extension()}) that this variable varies over. Use
\code{character(0)} for a scalar variable with no dimensions.}

\item{extent}{(optional) The extent of the values of the variable, as
\code{c(min, max)}.}

\item{values}{(optional) An explicit list of values, e.g. for variables
with a small number of distinct values.}

\item{unit}{(character, optional) The unit of measurement for the values.}

\item{nodata}{(optional) The no-data value(s) for the variable.}

\item{data_type}{(character, optional) The data type of the variable,
e.g. \code{"float32"}, \code{"int16"}.}

\item{description}{(character, optional) Detailed description of the
variable. CommonMark 0.29 syntax may be used for rich text
representation.}

\item{...}{Additional fields for the variable object.}
}
\value{
A named list of class \code{"cube_variable"}.
}
\description{
Creates a variable object for use with the Datacube Extension. Describes
a single variable stored in an N-dimensional data cube (e.g. a NetCDF
variable such as \code{temperature} or \code{precipitation}), including the
dimensions it varies over.
}
\examples{
temperature <- cube_variable(
  type = "data",
  dimensions = c("x", "y", "time"),
  unit = "degC",
  data_type = "float32"
)

quality_flag <- cube_variable(
  type = "auxiliary",
  dimensions = c("x", "y", "time"),
  data_type = "uint8"
)

}
