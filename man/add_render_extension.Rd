% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension-render.R
\name{add_render_extension}
\alias{add_render_extension}
\title{Add Render Extension to a STAC Item or Collection}
\usage{
add_render_extension(item, renders)
}
\arguments{
\item{item}{A STAC Item (\code{stac_item}) or Collection (\code{stac_collection})
object.}

\item{renders}{(named list, required) A named list of render objects
created with \code{\link[=render_object]{render_object()}}. Names are used as render keys and must
be unique.}
}
\value{
The modified STAC Item or Collection with Render extension fields
added.
}
\description{
Adds the STAC Render extension to a STAC Item or Collection. The Render
extension provides rendering hints that tell consumers how to visualize
the item's assets (e.g. on a web map).
}
\details{
\subsection{Extension Schema URI}{

The Render Extension v2.0.0 schema URI is:
\verb{https://stac-extensions.github.io/render/v2.0.0/schema.json}
}

\subsection{Field Placement}{
\itemize{
\item For \strong{Items}, \code{renders} is placed in \code{properties}.
\item For \strong{Collections}, \code{renders} is placed at the top level of the
Collection object.
}
}

\subsection{Render Keys}{

Each render object is stored under a unique key inside \code{renders}.
Common keys include \code{"thumbnail"}, \code{"true_color"}, \code{"ndvi"}, etc.
}
}
\examples{
item <- stac_item(
  id = "LC08_L1TP_044033_20210305",
  geometry = list(
    type = "Polygon",
    coordinates = list(list(
      c(-122.5, 39.5), c(-120.5, 39.5), c(-120.5, 40.5),
      c(-122.5, 40.5), c(-122.5, 39.5)
    ))
  ),
  bbox = c(-122.5, 39.5, -120.5, 40.5),
  datetime = "2021-03-05T18:45:37Z"
) |>
  add_asset(
    key = "B4",
    href = "https://example.com/B4.tif",
    type = "image/tiff; application=geotiff",
    roles = c("data")
  ) |>
  add_asset(
    key = "B3",
    href = "https://example.com/B3.tif",
    type = "image/tiff; application=geotiff",
    roles = c("data")
  ) |>
  add_asset(
    key = "B2",
    href = "https://example.com/B2.tif",
    type = "image/tiff; application=geotiff",
    roles = c("data")
  )

item <- item |>
  add_render_extension(renders = list(
    true_color = render_object(
      assets = c("B4", "B3", "B2"),
      title = "True Color",
      rescale = list(c(0, 10000), c(0, 10000), c(0, 10000)),
      resampling = "bilinear"
    )
  ))

}
\references{
Render Extension Specification:
\url{https://github.com/stac-extensions/render}
}
\seealso{
\itemize{
\item \code{\link[=render_object]{render_object()}} for creating render objects
\item \code{\link[=stac_item]{stac_item()}} for creating STAC Items
\item \code{\link[=stac_collection]{stac_collection()}} for creating STAC Collections
}
}
