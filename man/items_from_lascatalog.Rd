% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrations-lidr.R
\name{items_from_lascatalog}
\alias{items_from_lascatalog}
\title{Create STAC Items from a LAScatalog}
\usage{
items_from_lascatalog(ctg, datetime_from_filename = NULL, ...)
}
\arguments{
\item{ctg}{A \code{LAScatalog} object, a directory containing LAS/LAZ files, or a
character vector of file paths.}

\item{datetime_from_filename}{Function to extract a datetime from a filename.
Should return an ISO 8601 string. If NULL, the LAS header creation date is
used where available.}

\item{...}{Additional arguments passed to \code{item_from_lidr()}.}
}
\value{
A list of STAC Item objects.
}
\description{
Creates one STAC Item per file in a \code{lidR} \code{LAScatalog}, which is the usual
way a tiled point cloud collection is described. Only file headers are read,
so a catalogue of thousands of tiles can be catalogued quickly.
}
\details{
Files that cannot be read produce a warning and are skipped, so one damaged
tile does not abandon the whole catalogue. Pass the result to
\code{\link[=extent_from_items]{extent_from_items()}} to build the matching Collection extent.
}
\examples{
\dontrun{
library(lidR)

ctg <- readLAScatalog("path/to/tiles")
items <- items_from_lascatalog(ctg)

collection <- stac_collection(
  id = "als-tiles",
  description = "Airborne laser scanning tiles",
  license = "CC-BY-4.0",
  extent = extent_from_items(items)
)
}

}
\seealso{
\code{\link[=item_from_lidr]{item_from_lidr()}}, \code{\link[=extent_from_items]{extent_from_items()}}
}
