% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coercion.R
\name{st_as_sf.stac_catalog}
\alias{st_as_sf.stac_catalog}
\alias{st_as_sf.stac_item}
\title{Coerce a STAC Catalog, Collection or Item to an sf Object}
\usage{
\method{st_as_sf}{stac_catalog}(x, ..., resolve = FALSE, base_path = ".")

\method{st_as_sf}{stac_item}(x, ...)
}
\arguments{
\item{x}{A \code{stac_catalog}, \code{stac_collection} or \code{stac_item} object.}

\item{...}{Ignored.}

\item{resolve}{(logical) When the Items are not held in memory — as after
\code{\link[=read_stac]{read_stac()}} — read each one from its \code{item} link. Default \code{FALSE}.}

\item{base_path}{(character) Directory that relative item hrefs are resolved
against when \code{resolve = TRUE}. Default \code{"."}.}
}
\value{
An \code{sf} object with one row per Item, in EPSG:4326.
}
\description{
Returns the table that \link[=as.data.frame.stac_catalog]{as.data.frame()} gives,
with the Item footprints attached as a geometry column. This is the shape
most spatial work in R wants: filter on properties, plot the footprints, join
against other layers.

STAC types an Item's \code{geometry} as GeoJSON in WGS84 whatever projection the
underlying data uses, so the result is always in EPSG:4326. The native CRS,
when an Item records one, is in its \code{proj:code} property — see
\code{\link[=add_projection_extension]{add_projection_extension()}}.

An Item with a null geometry, which STAC allows for non-spatial data, gets an
empty geometry rather than being dropped.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE)) withAutoprint(\{ # examplesIf}
item <- stac_item(
  id = "scene-1",
  geometry = list(
    type = "Polygon",
    coordinates = list(list(
      c(-114, 51), c(-113, 51), c(-113, 52), c(-114, 52), c(-114, 51)
    ))
  ),
  bbox = c(-114, 51, -113, 52),
  datetime = "2024-06-01T00:00:00Z",
  properties = list(`eo:cloud_cover` = 7.5)
)

scenes <- sf::st_as_sf(item)
sf::st_bbox(scenes)
\dontshow{\}) # examplesIf}
}
