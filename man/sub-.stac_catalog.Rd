% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coercion.R
\name{[.stac_catalog}
\alias{[.stac_catalog}
\alias{[[.stac_catalog}
\title{Extract Items from a STAC Catalog or Collection}
\usage{
\method{[}{stac_catalog}(x, i)

\method{[[}{stac_catalog}(x, i)
}
\arguments{
\item{x}{A \code{stac_catalog} or \code{stac_collection} object.}

\item{i}{Item positions, or Item \code{id}s as a character vector.}
}
\value{
\code{[} a list of \code{stac_item} objects; \code{[[} a single \code{stac_item}.
}
\description{
Treats a Catalog or Collection as the container of Items that \code{length()}
reports, so Items can be pulled out by position or by \code{id}.
\itemize{
\item \code{x[i]} returns a list of Items.
\item \code{x[[i]]} returns a single Item.
}

Both accept a character index, matched against Item \code{id}s. Subsetting reads
the Items held in memory; it does not follow \code{item} links to disk, so it
returns nothing for a catalog read back with \code{\link[=read_stac]{read_stac()}}. Use
\code{\link[=get_items]{get_items()}} with \code{resolve = TRUE} for that.
}
\examples{
collection <- stac_collection(
  id = "scenes",
  description = "Example",
  license = "CC-BY-4.0",
  extent = stac_extent(
    spatial_bbox = list(c(-115, 50, -113, 52)),
    temporal_interval = list(list("2024-01-01T00:00:00Z", NULL))
  )
)
collection <- add_item(collection, stac_item(
  id = "scene-1",
  geometry = list(type = "Point", coordinates = c(-114, 51)),
  bbox = c(-114, 51, -114, 51),
  datetime = "2024-06-01T00:00:00Z"
))

collection[["scene-1"]]@id
length(collection[1])

}
