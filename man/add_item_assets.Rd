% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collection.R
\name{add_item_assets}
\alias{add_item_assets}
\title{Add Item Asset Definitions to a Collection}
\usage{
add_item_assets(collection)
}
\arguments{
\item{collection}{A \code{stac_collection} object with items added via
\code{add_item()}.}
}
\value{
The collection with \code{item_assets} populated and the Item Assets
extension added to \code{stac_extensions}.
}
\description{
Inspects the items already added to a collection and derives the
\code{item_assets} field automatically, using the assets present on those items.
For each unique asset key, the definition is taken from the first item that
contains it (minus the \code{href} field, which is item-specific). The
Item Assets extension URI is added to \code{stac_extensions} automatically.
}
\examples{
\dontrun{
collection <- stac_collection(
  id = "landsat",
  description = "Landsat imagery",
  license = "proprietary",
  extent = stac_extent(
    spatial_bbox = list(c(-180, -90, 180, 90)),
    temporal_interval = list(list("2020-01-01T00:00:00Z", NULL))
  )
)

item <- stac_item(
  id = "LC09_001",
  geometry = list(type = "Point", coordinates = c(-120, 48)),
  bbox = c(-121, 47, -119, 49),
  datetime = "2023-07-01T00:00:00Z"
)
item <- add_asset(item, key = "red",
  href = "red.tif", type = "image/tiff", roles = "data", title = "Red Band")

collection <- add_item(collection, item)
collection <- add_item_assets(collection)
}

}
\references{
STAC Item Assets Definition Extension:
\url{https://stac-extensions.github.io/item-assets/v1.0.0/schema.json}
}
\seealso{
\itemize{
\item \code{\link[=add_item]{add_item()}} for adding items to a collection
\item \code{\link[=stac_collection]{stac_collection()}} for creating collections
}
}
