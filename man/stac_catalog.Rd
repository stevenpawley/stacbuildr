% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{stac_catalog}
\alias{stac_catalog}
\title{Create a STAC Catalog}
\usage{
stac_catalog(
  id,
  description,
  title = NULL,
  stac_version = "1.1.0",
  type = "Catalog",
  stac_extensions = NULL,
  conformsTo = NULL,
  links = list(),
  ...
)
}
\arguments{
\item{id}{(character, required) Identifier for the Catalog. Must be unique
within the parent catalog if one exists. Should contain only alphanumeric
characters, hyphens, and underscores. This field is required by the STAC
specification.}

\item{description}{(character, required) Detailed multi-line description to
fully explain the Catalog. This field should provide comprehensive
information about the catalog's contents, purpose, and scope. This field is
required by the STAC specification.}

\item{title}{(character, optional) A short descriptive one-line title for the
Catalog. Recommended for human-readable identification.}

\item{stac_version}{(character, required) The STAC version the Catalog
implements. Defaults to \code{"1.1.0"}. This field is required by the STAC
specification.}

\item{type}{(character, optional) Must be set to \code{"Catalog"} for catalogs.
Defaults to \code{"Catalog"}. For collections, this would be \code{"Collection"}.
This field is required by the STAC specification.}

\item{stac_extensions}{(character vector, optional) A list of extension
URLs that the Catalog implements. Extensions listed here must
only contain extensions that extend the Catalog specification itself, not
extensions for Items or Collections. Each extension should be a full URI to
the extension's JSON schema. Default is \code{NULL} (no extensions).}

\item{conformsTo}{(character vector, optional) A list of URIs declaring
conformance to STAC API specifications or other standards. Typically used
when the catalog is served via an API. Introduced in STAC 1.1.0. Default
is \code{NULL}.}

\item{...}{Additional fields to include in the catalog. Any extra named
arguments will be added to the catalog object. This allows for custom
extensions or additional metadata beyond the core specification.}
}
\value{
An S7 object of class \code{stac_catalog} containing the catalog metadata.
Convert to a plain list for JSON serialization with \code{as.list()}, or write
directly to disk using \code{write_stac()}.
}
\description{
Creates a STAC (SpatioTemporal Asset Catalog) Catalog object following the
STAC specification version 1.1.0. A Catalog is a top-level organizational
structure that groups related Collections and Items, providing a hierarchical
structure for organizing geospatial assets, making them indexable and
discoverable.
}
\details{
\subsection{Required Fields}{

The STAC Catalog specification requires the following fields:
\itemize{
\item \code{type}: Must be "Catalog"
\item \code{stac_version}: STAC specification version (currently "1.1.0")
\item \code{id}: Unique identifier for the catalog
\item \code{description}: Detailed description of the catalog
}
}

\subsection{Recommended Fields}{
\itemize{
\item \code{title}: Short, human-readable title
}
}

\subsection{Link Relations}{

Catalogs use links to connect to other STAC resources. Common link relation
types include:
\itemize{
\item \code{root}: URL to the root STAC Catalog or Collection
\item \code{self}: Absolute URL to the current catalog file
\item \code{parent}: URL to the parent STAC Catalog or Collection
\item \code{child}: URL to a child STAC Catalog or Collection
\item \code{item}: URL to a STAC Item
}

Use the helper functions \code{add_self_link()}, \code{add_root_link()},
\code{add_parent_link()}, \code{add_child()}, and \code{add_item()} to manage links after
creating the catalog. A \code{self} link and a \code{root} link are strongly
recommended. Non-root Catalogs should include a \code{parent} link.
}

\subsection{Extensions}{

STAC extensions provide additional fields and capabilities. When using
extensions at the catalog level, reference them in the \code{stac_extensions}
parameter with their full schema URI. Note that most extensions apply to
Items or Collections rather than Catalogs.
}
}
\examples{
# Create a basic catalog
catalog <- stac_catalog(
  id = "my-catalog",
  description = "A catalog of satellite imagery for environmental monitoring"
)

# Create a catalog with all optional fields
catalog <- stac_catalog(
  id = "north-america-imagery",
  title = "North America Satellite Imagery",
  description = paste(
    "A comprehensive catalog of satellite imagery covering North America",
    "from various sensors including Landsat, Sentinel, and commercial",
    "providers. Data spans from 2013 to present."
  ),
  stac_version = "1.1.0"
)

# Add links to the catalog
catalog <- catalog |>
  add_self_link("https://example.com/catalog.json") |>
  add_root_link("https://example.com/catalog.json")

# Add child catalogs
landsat_catalog <- stac_catalog(
  id = "landsat",
  description = "Landsat satellite imagery"
)

catalog <- add_child(
  catalog,
  landsat_catalog,
  href = "./landsat/catalog.json",
  title = "Landsat Imagery"
)

# Create a catalog with a custom extension
catalog_with_version <- stac_catalog(
  id = "versioned-catalog",
  description = "A catalog with version tracking",
  stac_extensions = c(
    "https://stac-extensions.github.io/version/v1.2.0/schema.json"
  ),
  # Custom fields from the version extension
  version = "1.0.0",
  deprecated = FALSE
)

# Convert to JSON
catalog_json <- jsonlite::toJSON(catalog, auto_unbox = TRUE, pretty = TRUE)
cat(catalog_json)

}
\references{
STAC Catalog Specification:
\url{https://github.com/radiantearth/stac-spec/blob/master/catalog-spec/catalog-spec.md}
}
\seealso{
\itemize{
\item \code{\link[=stac_collection]{stac_collection()}} for creating STAC Collections
\item \code{\link[=stac_item]{stac_item()}} for creating STAC Items
\item \code{\link[=add_link]{add_link()}} for adding links to catalogs
\item \code{\link[=add_child]{add_child()}} for adding child catalogs or collections
\item \code{\link[=write_stac]{write_stac()}} for writing catalogs to the filesystem
}
}
