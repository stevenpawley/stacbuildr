% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coercion.R
\name{as.data.frame.stac_catalog}
\alias{as.data.frame.stac_catalog}
\alias{as.data.frame.stac_item}
\title{Coerce a STAC Catalog, Collection or Item to a Data Frame}
\usage{
\method{as.data.frame}{stac_catalog}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  resolve = FALSE,
  base_path = "."
)

\method{as.data.frame}{stac_item}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A \code{stac_catalog}, \code{stac_collection} or \code{stac_item} object.}

\item{row.names, optional}{Ignored, for compatibility with the generic.}

\item{...}{Ignored.}

\item{resolve}{(logical) When the Items are not held in memory — as after
\code{\link[=read_stac]{read_stac()}} — read each one from its \code{item} link. Default \code{FALSE}.}

\item{base_path}{(character) Directory that relative item hrefs are resolved
against when \code{resolve = TRUE}. Default \code{"."}.}
}
\value{
A data frame with one row per Item. A catalog with no Items gives a
zero-row data frame; if it has \code{item} links but no Items in memory, a
warning points at \code{resolve = TRUE}.
}
\description{
Returns one row per Item, with \code{id} and \code{collection} followed by a column for
every field in the Items' \code{properties}. Items need not share the same
properties: a field missing from an Item is \code{NA} in its row.

A property whose values are not all length-1 atomics becomes a list column,
since STAC properties routinely hold vectors (\code{proj:transform}) and objects
(\code{bands}). Use \link[sf:st_as_sf]{st_as_sf()} instead to get the same table with
the Item footprints attached as a geometry column.
}
\examples{
item <- stac_item(
  id = "scene-1",
  geometry = list(type = "Point", coordinates = c(-114, 51)),
  bbox = c(-114, 51, -114, 51),
  datetime = "2024-06-01T00:00:00Z",
  properties = list(`eo:cloud_cover` = 7.5)
)

as.data.frame(item)

collection <- stac_collection(
  id = "scenes",
  description = "Example",
  license = "CC-BY-4.0",
  extent = stac_extent(
    spatial_bbox = list(c(-115, 50, -113, 52)),
    temporal_interval = list(list("2024-01-01T00:00:00Z", NULL))
  )
)
collection <- add_item(collection, item)

df <- as.data.frame(collection)
df[df$`eo:cloud_cover` < 20, c("id", "datetime")]

}
