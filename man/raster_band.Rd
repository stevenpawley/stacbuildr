% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension-raster.R
\name{raster_band}
\alias{raster_band}
\title{Creates a band object for use with the Raster Extension. Describes the
characteristics of a single raster band including data type, nodata values,
scale/offset transforms, and statistics.}
\usage{
raster_band(
  nodata = NULL,
  data_type = NULL,
  unit = NULL,
  statistics = NULL,
  sampling = NULL,
  bits_per_sample = NULL,
  spatial_resolution = NULL,
  scale = 1,
  offset = 0,
  histogram = NULL,
  ...
)
}
\arguments{
\item{nodata}{(numeric or NULL, optional) Pixel value(s) that should be
interpreted as "no data". Can be a single value or vector of values. Common
values: 0, -9999, NaN.}

\item{data_type}{(character, optional) Data type of the band. Must be one of:
"int8", "int16", "int32", "int64", "uint8", "uint16", "uint32", "uint64",
"float16", "float32", "float64", "cint16", "cint32", "cfloat32",
"cfloat64", or "other".}

\item{unit}{(character, optional) Unit of measurement for the pixel values.
Examples: "m" (meters), "W sr-1 m-2" (radiance), "1"
(unitless/reflectance).}

\item{statistics}{(list, optional) Statistics object created with
\code{raster_statistics()} describing the distribution of pixel values.}

\item{sampling}{single length character, must be either 'point' where the
pixel value represents a point sample at the centre of the pixel, or 'area'
where the pixel value should be assumed to represent a sampling over the
region of the pixel}

\item{bits_per_sample}{(integer, optional) Actual number of bits used for
this band. Only needed when different from the standard for the data type
(e.g., 1-bit data stored in uint8).}

\item{spatial_resolution}{(numeric, optional) Average spatial resolution of
pixels in the band, in meters. Useful when resolution varies or differs
from ground sample distance (gsd).}

\item{scale}{(numeric, optional) Multiplicative scaling factor to transform
pixel values: \code{physical_value = scale * DN + offset}. Default is 1.}

\item{offset}{(numeric, optional) Additive offset to transform pixel values:
\code{physical_value = scale * DN + offset}. Default is 0.}

\item{histogram}{(list, optional) Histogram object created with
\code{raster_histogram()} describing the distribution of pixel values.}

\item{...Additional}{fields for the band object. Can include fields from
other extensions like \code{"eo:common_name"}, \code{"eo:center_wavelength"}.}
}
\value{
An S7 class representing a raster band object.
}
\description{
\code{raster_band()} is an S7 object that is used to construct a \code{raster:bands}
STAC metadata entry
}
