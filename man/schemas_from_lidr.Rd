% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrations-lidr.R
\name{schemas_from_lidr}
\alias{schemas_from_lidr}
\title{Build Point Cloud Schema Objects from a LAS Header}
\usage{
schemas_from_lidr(header)
}
\arguments{
\item{header}{A \code{LASheader} object from \code{lidR::readLASheader()}, or a \code{LAS}
object (its header is used).}
}
\value{
A list of \code{\link[=pc_schema]{pc_schema()}} objects, or \code{NULL} if the point data record
format is not recognised.
}
\description{
Derives the \code{pc:schemas} dimension list for a LAS/LAZ file from its point
data record format, plus any additional dimensions declared in the file's
Extra Bytes variable length record.
}
\details{
Only the header is read, so this is fast even for very large files. Sizes
follow the whole-byte, unpacked convention described in
\code{\link[=add_pointcloud_extension]{add_pointcloud_extension()}}.
}
\examples{
\dontrun{
header <- lidR::readLASheader("points.laz")
schemas_from_lidr(header)
}

}
\seealso{
\code{\link[=item_from_lidr]{item_from_lidr()}}, \code{\link[=add_pointcloud_extension]{add_pointcloud_extension()}}
}
