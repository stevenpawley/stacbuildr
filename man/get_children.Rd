% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write.R
\name{get_children}
\alias{get_children}
\title{Get Stored Children from Catalog}
\usage{
get_children(catalog, resolve = FALSE, base_path = ".")
}
\arguments{
\item{catalog}{A STAC Catalog or Collection object.}

\item{resolve}{(logical) If \code{TRUE} and no children are stored in memory,
follow the \code{child} links and read each one from disk. Relative hrefs are
resolved against \code{base_path}. Default is \code{FALSE}.}

\item{base_path}{(character) Directory used to resolve relative hrefs when
\code{resolve = TRUE}. Defaults to the working directory.}
}
\value{
A named list of child catalogs/collections, or NULL if none exist.
}
\description{
Retrieves the stored child catalogs/collections from a catalog object.
When children are not in memory (e.g. after \code{read_stac()}), set
\code{resolve = TRUE} to follow the \code{child} links and load them from disk.
}
\examples{
\dontrun{
# In-memory children
children <- get_children(catalog)

# After read_stac(), follow links from disk
catalog <- read_stac("path/to/catalog.json")
children <- get_children(catalog, resolve = TRUE, base_path = "path/to")
names(children)
}

}
