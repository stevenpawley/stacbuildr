% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension-pointcloud.R
\name{pc_schema}
\alias{pc_schema}
\title{Create a Point Cloud Schema Object}
\usage{
pc_schema(name, size, type)
}
\arguments{
\item{name}{(character) \strong{Required.} The name of the dimension, e.g.
\code{"X"}, \code{"Intensity"}, or \code{"Classification"}.}

\item{size}{(integer) \strong{Required.} The size of the dimension in whole
bytes. Must be a whole number greater than 0.}

\item{type}{(character) \strong{Required.} The dimension type. One of
\code{"floating"}, \code{"unsigned"}, or \code{"signed"}.}
}
\value{
A \code{pc_schema} object (a list with a print method).
}
\description{
Creates a Schema object for the \code{pc:schemas} field of the Point Cloud
Extension. Each object describes one dimension (channel) of the point cloud:
its name, its size in whole bytes, and how its bytes are interpreted.
}
\details{
Only whole-byte sizes are representable. Bit-packed LAS fields such as
\code{ReturnNumber} are conventionally reported as the unpacked one-byte
dimension a reader materialises, which is what \code{\link[=item_from_lidr]{item_from_lidr()}} does.
}
\examples{
pc_schema("X", size = 8, type = "floating")
pc_schema("Intensity", size = 2, type = "unsigned")

}
\seealso{
\code{\link[=add_pointcloud_extension]{add_pointcloud_extension()}}, \code{\link[=pc_statistic]{pc_statistic()}}
}
