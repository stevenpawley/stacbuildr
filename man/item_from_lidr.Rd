% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrations-lidr.R
\name{item_from_lidr}
\alias{item_from_lidr}
\title{Create a STAC Item from a LAS/LAZ Point Cloud}
\usage{
item_from_lidr(
  x,
  href = NULL,
  id = NULL,
  datetime = NULL,
  properties = list(),
  assets = list(),
  asset_key = "data",
  asset_roles = c("data"),
  pc_type = "lidar",
  add_pointcloud = TRUE,
  add_projection = TRUE,
  add_schemas = TRUE,
  calculate_statistics = FALSE,
  reproject_to_wgs84 = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{LASheader} object, a \code{LAS} object, or a path to a LAS/LAZ file.}

\item{href}{(character, optional) URI for the point cloud asset. If provided,
the file is added as an asset and \code{id} is derived from the basename when not
explicitly set. Defaults to \code{x} when \code{x} is a file path.}

\item{id}{(character, optional) Item ID. If NULL, derived from \code{href}
basename.}

\item{datetime}{(character, optional) ISO 8601 datetime string. If NULL, the
file creation year and day of year from the LAS header are used; if those
are unset (which is common), the current time is used with a warning.}

\item{properties}{(list, optional) Additional properties for the item.}

\item{assets}{(list, optional) Additional assets beyond the point cloud.}

\item{asset_key}{(character, optional) Key name for the point cloud asset.
Default is "data".}

\item{asset_roles}{(character vector, optional) Roles for the point cloud
asset. Default is \code{c("data")}.}

\item{pc_type}{(character, optional) The \code{pc:type} phenomenology value.
Default is \code{"lidar"}.}

\item{add_pointcloud}{(logical, optional) If TRUE, adds the Point Cloud
extension. Default is TRUE.}

\item{add_projection}{(logical, optional) If TRUE and the file declares a CRS,
adds Projection extension fields in the file's native CRS. Default is TRUE.}

\item{add_schemas}{(logical, optional) If TRUE, derives \code{pc:schemas} from the
point data record format and Extra Bytes VLR. Default is TRUE.}

\item{calculate_statistics}{(logical, optional) If TRUE, computes full
per-channel \code{pc:statistics} (average, stddev, variance and so on). This
reads every point and can be slow for large files. When FALSE, only the
X/Y/Z bounds carried in the header are recorded. Default is FALSE.}

\item{reproject_to_wgs84}{(logical, optional) If TRUE and the file is not in
WGS84, reprojects the extent to EPSG:4326, which STAC requires. Default is
TRUE.}

\item{...}{Additional arguments passed to \code{stac_item()}.}
}
\value{
A STAC Item object with the point cloud metadata.
}
\description{
Creates a STAC Item from a point cloud read with the \code{lidR} package,
populating the Point Cloud extension from the file's public header block.
Geometry, bounding box, projection metadata, point count, density, and the
dimension schema are all derived from the header alone, so items can be built
from very large files without reading a single point.
}
\details{
\subsection{Statistics}{

\code{calculate_statistics = FALSE} still records \code{minimum}, \code{maximum} and \code{count}
for the X, Y and Z channels, because the LAS public header block carries those
bounds. The remaining statistics, and statistics for any other channel,
require a full read of the points.
}

\subsection{Point Density}{

\code{pc:density} is \code{lidR}'s point density: the point count divided by the area of
the file's bounding rectangle, in the units of the file's own CRS. For data in
a projected CRS in metres this is points per square metre.
}
}
\examples{
\dontrun{
library(lidR)

f <- system.file("extdata", "Megaplot.laz", package = "lidR")

# Header only: fast even for very large files
item <- item_from_lidr(f)

# With full per-channel statistics, which reads the points
item <- item_from_lidr(
  f,
  href = "https://example.com/Megaplot.laz",
  datetime = "2023-06-15T10:30:00Z",
  calculate_statistics = TRUE
)
}

}
\seealso{
\itemize{
\item \code{\link[=items_from_lascatalog]{items_from_lascatalog()}} for building items for a whole tiled collection
\item \code{\link[=add_pointcloud_extension]{add_pointcloud_extension()}} to set the fields by hand
\item \code{\link[=schemas_from_lidr]{schemas_from_lidr()}} for just the dimension schema
}
}
