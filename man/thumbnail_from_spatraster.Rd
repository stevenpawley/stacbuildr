% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thumbnail.R
\name{thumbnail_from_spatraster}
\alias{thumbnail_from_spatraster}
\title{Generate a Thumbnail PNG from a SpatRaster Object}
\usage{
thumbnail_from_spatraster(
  spat_rast,
  path,
  width = 256,
  height = 256,
  title = NULL,
  ...
)
}
\arguments{
\item{spat_rast}{A \code{SpatRaster} object.}

\item{path}{(character, required) File path for the output PNG.}

\item{width}{(integer) Image width in pixels. Default is 256.}

\item{height}{(integer) Image height in pixels. Default is 256.}

\item{title}{(character, optional) Title for the returned asset.}

\item{...}{Additional arguments passed to \code{terra::plotRGB()} or
\code{terra::plot()}.}
}
\value{
A STAC asset list with \code{href}, \code{type = "image/png"}, and
\code{roles = c("thumbnail")}.
}
\description{
Renders a \code{terra} \code{SpatRaster} to a PNG image and returns a STAC asset
pointing to it. Rasters with 3 or more layers are rendered as an RGB
composite using layers 1–3 via \code{terra::plotRGB()}; single-layer rasters
are rendered as a greyscale image via \code{terra::plot()}.
}
\examples{
\dontrun{
library(terra)

r <- rast(system.file("ex/logo.tif", package = "terra"))
asset <- thumbnail_from_spatraster(r, path = "thumbnail.png")

item <- item_from_spatraster(r, id = "logo", datetime = "2023-01-01T00:00:00Z")
item <- add_asset(item, key = "thumbnail", asset = asset)
}

}
