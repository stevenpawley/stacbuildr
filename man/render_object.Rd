% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension-render.R
\name{render_object}
\alias{render_object}
\title{Create a STAC Render Object}
\usage{
render_object(
  assets,
  title = NULL,
  rescale = NULL,
  nodata = NULL,
  colormap_name = NULL,
  colormap = NULL,
  color_formula = NULL,
  resampling = NULL,
  expression = NULL,
  minmax_zoom = NULL,
  bidx = NULL,
  ...
)
}
\arguments{
\item{assets}{(character, required) Asset keys referencing the assets that
are used to make the rendering. These must be local asset keys defined
in the same STAC Item.}

\item{title}{(character, optional) Title of the rendering.}

\item{rescale}{(list, optional) A list of numeric vectors, each of length 2,
giving the minimum and maximum range per band. For example,
\code{list(c(0, 10000), c(0, 10000), c(0, 10000))} for an RGB composite.}

\item{nodata}{(numeric or character, optional) Nodata value to use for the
referenced assets.}

\item{colormap_name}{(character, optional) Named color map to apply to a
raster band (e.g. \code{"ylgn"}, \code{"rainbow"}).}

\item{colormap}{(list, optional) A custom color map JSON definition.}

\item{color_formula}{(character, optional) Color formula to apply to a
raster band (see TiTiler documentation for examples).}

\item{resampling}{(character, optional) GDAL resampling algorithm to apply
to the referenced assets (e.g. \code{"nearest"}, \code{"bilinear"}, \code{"average"}).}

\item{expression}{(character, optional) Band arithmetic formula to apply to
the referenced assets (e.g. \code{"(B5-B4)/(B5+B4)"}).}

\item{minmax_zoom}{(numeric, optional) Zoom level range applicable for the
visualization, as a length-2 numeric vector \code{c(min_zoom, max_zoom)}.}

\item{bidx}{(numeric, optional) Band indexes to use for rendering.}

\item{...}{Additional fields allowed by the open-ended Render Object schema.}
}
\value{
A named list of class \code{"render_object"}.
}
\description{
Creates a render object for use with the STAC Render Extension. A render
object describes how one or more assets should be visualized (e.g. in a
web map or dynamic tile server).
}
\details{
\subsection{Render Object Fields}{

The Render Extension defines the following standard fields inside each
render object:
\itemize{
\item \code{assets}: Required. Asset keys used for rendering.
\item \code{title}: Optional human-readable title.
\item \code{rescale}: Optional per-band min/max ranges.
\item \code{nodata}: Optional nodata value.
\item \code{colormap_name}: Optional named color map.
\item \code{colormap}: Optional custom color map definition.
\item \code{color_formula}: Optional color formula.
\item \code{resampling}: Optional resampling method.
\item \code{expression}: Optional band arithmetic expression.
\item \code{minmax_zoom}: Optional zoom level range.
\item \code{bidx}: Optional band indexes.
}

Additional fields may be supplied via \code{...}.
}
}
\examples{
rgb_render <- render_object(
  assets = c("B4", "B3", "B2"),
  title = "True Color",
  rescale = list(c(0, 10000), c(0, 10000), c(0, 10000)),
  resampling = "bilinear"
)

ndvi_render <- render_object(
  assets = c("B5", "B4"),
  title = "NDVI",
  expression = "(B5-B4)/(B5+B4)",
  rescale = list(c(-1, 1)),
  colormap_name = "ylgn",
  resampling = "average"
)

}
