% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension-vector.R
\name{add_vector_extension}
\alias{add_vector_extension}
\title{Add Vector Extension to a STAC Item}
\usage{
add_vector_extension(
  item,
  geometry_types = NULL,
  mmu = NULL,
  mmw = NULL,
  reference_scale = NULL,
  asset_key = NULL
)
}
\arguments{
\item{item}{A STAC Item object created with \code{stac_item()}.}

\item{geometry_types}{(character, optional) A vector of the geometry types
present in the dataset. Must be one or more of \code{"Point"},
\code{"MultiPoint"}, \code{"LineString"}, \code{"MultiLineString"}, \code{"Polygon"},
\code{"MultiPolygon"}, or \code{"GeometryCollection"}. Each type may only appear
once.}

\item{mmu}{(numeric, optional) Minimum Mapping Unit: the area, in square
meters, of the smallest polygon represented in the dataset. Must be
greater than 0.}

\item{mmw}{(numeric, optional) Minimal Mapping Width: the width, in
meters, of the smallest real-world feature represented as a polygon in
the dataset. Must be greater than 0.}

\item{reference_scale}{(numeric, optional) The representative fraction
denominator of the scale that the data was originally digitized or
captured at (e.g. \code{50000} for a scale of 1:50,000). Must be greater than
0.}

\item{asset_key}{(character, optional) If provided, adds the vector fields
to a specific asset rather than to the item properties. Useful when
different assets within an item represent vector data digitized at
different scales or containing different geometry types.}
}
\value{
The modified STAC Item with Vector extension fields added.
}
\description{
Adds the Vector Extension to a STAC Item. The Vector Extension describes
basic properties and metrics of vector data (geometries with additional
properties), such as which geometry types are present in the dataset and
the minimum mapping unit/width used when the data was digitized.
}
\details{
\subsection{Extension Schema URI}{

The Vector Extension v0.1.0 schema URI is:
\verb{https://stac-extensions.github.io/vector/v0.1.0/schema.json}
}

\subsection{Field Placement}{

All four fields may be placed either on item properties (the default) or
on a specific asset via \code{asset_key}. The extension also allows these
fields to be set on individual Table Column objects (see
\code{\link[=table_column]{table_column()}}) — pass them as extra named arguments to \code{table_column()}
(e.g. \code{"vector:geometry_types" = "Point"}) when a table has multiple
geometry columns with different characteristics.
}

\subsection{Companion Extensions}{

The Vector Extension is commonly used alongside the \strong{Table Extension}
(see \code{\link[=add_table_extension]{add_table_extension()}}) for tabular/columnar vector datasets such as
GeoParquet.
}
}
\examples{
item <- stac_item(
  id = "my-vector-dataset",
  geometry = list(
    type = "Polygon",
    coordinates = list(list(
      c(-105.5, 39.5), c(-104.5, 39.5), c(-104.5, 40.5),
      c(-105.5, 40.5), c(-105.5, 39.5)
    ))
  ),
  bbox = c(-105.5, 39.5, -104.5, 40.5),
  datetime = "2023-06-15T00:00:00Z"
)

item <- item |>
  add_vector_extension(
    geometry_types  = c("Polygon", "MultiPolygon"),
    mmu             = 100,
    reference_scale = 50000
  )

}
\references{
Vector Extension Specification:
\url{https://github.com/stac-extensions/vector}
}
\seealso{
\itemize{
\item \code{\link[=add_table_extension]{add_table_extension()}} for describing tabular vector datasets
\item \code{\link[=table_column]{table_column()}} for creating table column objects
\item \code{\link[=stac_item]{stac_item()}} for creating STAC Items
}
}
