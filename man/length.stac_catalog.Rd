% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coercion.R
\name{length.stac_catalog}
\alias{length.stac_catalog}
\title{Number of Items in a STAC Catalog or Collection}
\usage{
\method{length}{stac_catalog}(x)
}
\arguments{
\item{x}{A \code{stac_catalog} or \code{stac_collection} object.}
}
\value{
An integer count of linked Items.
}
\description{
Returns the number of Items linked from a Catalog or Collection, so that a
catalog answers \code{length()} the way a container of Items should. This is the
same count as \code{\link[=count_items]{count_items()}}, which counts \code{item} links rather than the
Item objects held in memory, so it is correct for a catalog read back from
disk as well as one built in the session.

Child catalogs are not counted; use \code{length(get_children(x))} for those.
}
\examples{
catalog <- stac_catalog(id = "my-catalog", description = "Example")
length(catalog)

}
