% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coercion.R
\name{stac_item_sf_accessors}
\alias{stac_item_sf_accessors}
\alias{st_geometry.stac_item}
\alias{st_bbox.stac_item}
\alias{st_crs.stac_item}
\title{sf Accessors for a STAC Item}
\usage{
\method{st_geometry}{stac_item}(obj, ...)

\method{st_bbox}{stac_item}(obj, ...)

\method{st_crs}{stac_item}(x, ...)
}
\arguments{
\item{obj}{A \code{stac_item} object.}

\item{...}{Ignored.}

\item{x}{A \code{stac_item} object.}
}
\value{
\code{st_geometry()} an \code{sfc}; \code{st_bbox()} a \code{bbox}; \code{st_crs()} a \code{crs}.
}
\description{
A STAC Item is a GeoJSON Feature, so the \code{sf} accessors work on it directly.
\itemize{
\item \code{st_geometry()} returns the Item's footprint as a length-1 \code{sfc}.
\item \code{st_bbox()} returns its bounding box from the Item's \code{bbox} field. A STAC
Item must carry one whenever it has a geometry, so the empty bbox comes
back only for a non-spatial Item with neither.
\item \code{st_crs()} returns EPSG:4326. An Item's \code{geometry} and \code{bbox} are WGS84 by
specification whatever projection the underlying data uses; the native CRS,
when recorded, is in the \code{proj:code} property.
}
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE)) withAutoprint(\{ # examplesIf}
item <- stac_item(
  id = "scene-1",
  geometry = list(type = "Point", coordinates = c(-114, 51)),
  bbox = c(-114, 51, -114, 51),
  datetime = "2024-06-01T00:00:00Z"
)

sf::st_geometry(item)
sf::st_bbox(item)
sf::st_crs(item)$epsg
\dontshow{\}) # examplesIf}
}
