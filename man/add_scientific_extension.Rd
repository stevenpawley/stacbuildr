% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension-scientific.R
\name{add_scientific_extension}
\alias{add_scientific_extension}
\title{Add Scientific Citation Extension to a STAC Item}
\usage{
add_scientific_extension(
  item,
  doi = NULL,
  citation = NULL,
  publications = NULL
)
}
\arguments{
\item{item}{A STAC Item object created with \code{stac_item()}.}

\item{doi}{(character, optional) The DOI of the data, e.g.
\code{"10.1000/xyz123"}. This must \strong{not} be a full DOI URL — provide only the
DOI name. A corresponding \code{cite-as} link will be added automatically.}

\item{citation}{(character, optional) The recommended human-readable
reference (citation) to be used by publications citing the data. No
specific citation style is required, but the citation should contain
enough information to uniquely identify the publication.}

\item{publications}{(list, optional) A list of \code{\link[=scientific_publication]{scientific_publication()}}
objects describing related publications that reference or describe the
data.}
}
\value{
The modified STAC Item with Scientific Citation extension fields
added.
}
\description{
Adds the Scientific Citation Extension to a STAC Item. This extension
provides fields to indicate from which publication data originates and how
the data itself should be cited or referenced, helping to increase
reproducibility and citability.

Three fields are available, at least one of which must be supplied:
\itemize{
\item \strong{\code{sci:doi}}: The DOI of the dataset itself (e.g. \code{"10.1000/xyz123"}).
\item \strong{\code{sci:citation}}: A recommended human-readable citation string for the
dataset.
\item \strong{\code{sci:publications}}: A list of related publications created with
\code{\link[=scientific_publication]{scientific_publication()}}.
}

When a DOI is provided, a \code{cite-as} link pointing to the DOI URL is
automatically appended to the item's links list (per RFC 8574).
}
\details{
\subsection{Extension Schema URI}{

\verb{https://stac-extensions.github.io/scientific/v1.0.0/schema.json}
}

\subsection{DOI Format}{

DOIs should be supplied as bare names such as \code{"10.1000/xyz123"}, not as
full links (\verb{https://doi.org/10.1000/xyz123}). A \code{cite-as} link with the
full DOI URL is added to the item links automatically when \code{doi} is
provided.
}

\subsection{Placement}{

The scientific fields are placed in the item \code{properties}. Because citation
information is often shared across all items in a collection, the STAC
specification recommends adding these fields at the Collection level where
possible.
}
}
\examples{
item <- stac_item(
  id = "my-dataset",
  geometry = list(
    type = "Polygon",
    coordinates = list(list(
      c(-105.5, 39.5), c(-104.5, 39.5), c(-104.5, 40.5),
      c(-105.5, 40.5), c(-105.5, 39.5)
    ))
  ),
  bbox = c(-105.5, 39.5, -104.5, 40.5),
  datetime = "2023-01-01T00:00:00Z"
)

# Add citation with DOI and a related publication
item <- item |>
  add_scientific_extension(
    doi = "10.1000/xyz123",
    citation = "Smith, J. (2023). My Dataset. Example Repository.",
    publications = list(
      scientific_publication(
        doi = "10.1000/abc456",
        citation = "Smith, J. (2022). Methods paper. Journal of Examples."
      )
    )
  )

}
\references{
Scientific Citation Extension Specification:
\url{https://github.com/stac-extensions/scientific}
}
\seealso{
\itemize{
\item \code{\link[=scientific_publication]{scientific_publication()}} for creating publication objects
\item \code{\link[=stac_item]{stac_item()}} for creating STAC Items
}
}
