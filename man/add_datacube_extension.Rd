% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extension-datacube.R
\name{add_datacube_extension}
\alias{add_datacube_extension}
\title{Add Datacube Extension to a STAC Item}
\usage{
add_datacube_extension(
  item,
  dimensions = NULL,
  variables = NULL,
  asset_key = NULL
)
}
\arguments{
\item{item}{A STAC Item object created with \code{stac_item()}.}

\item{dimensions}{(named list, optional) A named list of dimension objects
created with \code{cube_dimension()}. Names are used as the dimension keys
(e.g. \code{"x"}, \code{"y"}, \code{"time"}) and must be unique.}

\item{variables}{(named list, optional) A named list of variable objects
created with \code{cube_variable()}. Names are used as the variable keys and
must be unique, and must not clash with any name used in \code{dimensions}.}

\item{asset_key}{(character, optional) If provided, adds the datacube
fields to a specific asset rather than to the item properties. Useful
when different assets within an item (e.g. separate NetCDF files)
describe different data cubes.}
}
\value{
The modified STAC Item with Datacube extension fields added.
}
\description{
Adds the Datacube Extension to a STAC Item. The Datacube Extension
describes N-dimensional data cubes (e.g. NetCDF, Zarr, multi-band raster
stacks) by specifying the dimensions of the cube (spatial, temporal, or
additional custom dimensions) and, optionally, the variables it contains.
}
\details{
\subsection{Extension Schema URI}{

The Datacube Extension v2.3.0 schema URI is:
\verb{https://stac-extensions.github.io/datacube/v2.3.0/schema.json}
}

\subsection{Field Placement}{

\code{cube:dimensions} and \code{cube:variables} may be placed either on item
properties (the default) or on a specific asset via \code{asset_key}.
}

\subsection{Key Uniqueness}{

The keys of \code{dimensions} and \code{variables} should be unique together; a key
such as \code{"lat"} should not be used for both a dimension and a variable.
}
}
\examples{
item <- stac_item(
  id = "my-datacube",
  geometry = list(
    type = "Polygon",
    coordinates = list(list(
      c(-105.5, 39.5), c(-104.5, 39.5), c(-104.5, 40.5),
      c(-105.5, 40.5), c(-105.5, 39.5)
    ))
  ),
  bbox = c(-105.5, 39.5, -104.5, 40.5),
  datetime = "2023-06-15T00:00:00Z"
)

dims <- list(
  x = cube_dimension(
    type = "spatial", axis = "x", extent = c(-105.5, -104.5),
    reference_system = 4326
  ),
  y = cube_dimension(
    type = "spatial", axis = "y", extent = c(39.5, 40.5),
    reference_system = 4326
  ),
  time = cube_dimension(
    type = "temporal",
    extent = c("2023-06-01T00:00:00Z", "2023-06-30T00:00:00Z")
  )
)

vars <- list(
  temperature = cube_variable(
    type = "data",
    dimensions = c("x", "y", "time"),
    unit = "degC",
    data_type = "float32"
  )
)

item <- item |>
  add_datacube_extension(dimensions = dims, variables = vars)

}
\references{
Datacube Extension Specification:
\url{https://github.com/stac-extensions/datacube}
}
\seealso{
\itemize{
\item \code{\link[=cube_dimension]{cube_dimension()}} for creating dimension objects
\item \code{\link[=cube_variable]{cube_variable()}} for creating variable objects
\item \code{\link[=stac_item]{stac_item()}} for creating STAC Items
}
}
