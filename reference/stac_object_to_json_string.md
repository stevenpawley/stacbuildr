# Serialise a STAC S7 object to a JSON string, mirroring the write\_\* pattern.

Serialise a STAC S7 object to a JSON string, mirroring the write\_\*
pattern.

## Usage

``` r
stac_object_to_json_string(stac_object)
```

## Arguments

- stac_object:

  STAC object
