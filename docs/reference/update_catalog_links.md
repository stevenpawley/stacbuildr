# Update Catalog Links for Filesystem Structure

Internal function to update links in a catalog to match the filesystem
structure.

## Usage

``` r
update_catalog_links(
  catalog,
  path,
  catalog_type,
  base_url = NULL,
  is_root = FALSE,
  parent_href = NULL
)
```
